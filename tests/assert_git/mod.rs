/// Inspired by `assert_cmd`. Asserts on a git repo.

mod assert {
    /// The point is a builder pattern on git repos.
    pub struct Assert {}

    impl Repo {
        // pub fn get_commits(branch: str) ->
    }
}
